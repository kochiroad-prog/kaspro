// Shared layout for all protected pages (transaksi, kas, etc.)
// Copy dari dashboard/layout.tsx — semua halaman dalam folder ini
// menggunakan layout yang sama.

export { default } from '../dashboard/layout'
